# DNS_RECORD_OWNER_ID (optional)
# Unique identifier for DNS record ownership.
# Only set this if you need multiple worker instances managing the same DNS zone.
# Default: cloudflare-tailscale-dns
#
# DNS_RECORD_OWNER_ID=cloudflare-tailscale-dns

# LOG_LEVEL (optional)
# Can be set here for startup logging, but the /config UI setting takes precedence.
# Valid values: DEBUG, INFO, WARN, ERROR
# Default: INFO
#
# LOG_LEVEL=INFO
